type Query {
  info: String!
  feed: [Poem!]!
}

type Mutation {
    post(title: String!, content: String!, authorId: Int!): Poem!
}

type User {
    id: ID!
    name: String!
    email: String!
    poems: [Poem!]!
}

type Poem {
    id: ID!
    title: String!
    content: String
    createdAt: String
    authorId: Int!
    author: User
}